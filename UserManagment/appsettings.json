{
  "ConnectionStrings": {
    "DbContext": "Server=localhost; Database= db_usermanagment; TrustServerCertificate=True; Integrated Security=True;"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}

/// why we use appsetting.json
// it used to all the configuration settings of the project for e.g jwt, sendgrid, twillo,connectionstring

//builder.configuration.GetConnectionString("DbContext")